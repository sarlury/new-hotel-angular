import { HotelFilterSortPaginatePipe } from './hotel-filter-sort-paginate.pipe';

describe('HotelFilterSortPaginatePipe', () => {
  it('create an instance', () => {
    const pipe = new HotelFilterSortPaginatePipe();
    expect(pipe).toBeTruthy();
  });
});
